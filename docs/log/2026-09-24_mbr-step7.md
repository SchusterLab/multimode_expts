# 2026-09-24: MBR redesign step 7 (disorder / SFF)

Session record; not edited after this date. The current state is `docs/STATUS.md`. The full
plan and its progress record are `docs/qsim/mbr_step7_plan.md`.

## Done

| Commit | What |
|---|---|
| `49ad952` | written plan, `docs/qsim/mbr_step7_plan.md` |
| `e9fdf85` | 7a: off-diagonal (pairwise, D72), SFF and shot-sampling code moved to `deprecated/` and `dormant/` |
| `e85e8f2` | 7b-7d: diagonal disorder on the new classes (`MBRDisorderEnsembleExperiment`, `fitting/qsim/mbr_disorder.py`, `mbr_hamiltonian.py`, migration kind `disorder`); August r0-r3 and 7-1 r0-r18 converted into the prod data tree |
| `271c17b` | 7e: old classes and programs moved to `deprecated/`; test that live code does not import it |

## Decisions (guan)

- Nothing is deleted in step 7; code leaving the canonical path moves to `deprecated/` or
  `dormant/`. guan and jonginn delete later.
- Only diagonal time traces are canonical. Off-diagonal (D72, pairwise) is out: the Stark-shift
  calibration cannot calibrate init != final (forward/backward Trotter gives about zero return),
  and off-diagonals add no levels that a complete diagonal basis does not give.
- SFF (`DisorderSFFExperiment`) is to be deleted; it will be written again from the start.
- Shot sampling is dormant code.
- Arguments that the copied notebook blocks ignored are copy hazards, not bugs to audit; the port
  connects the data flow.
- Sandbox notebooks (`guan/`, `jonginn/`) stay as they are, even if broken.
- Docs convention from this date: `docs/STATUS.md` (current, overwritten) + `docs/log/`
  (dated, append-only); see `AGENTS.md`.

## Found

- The step-6 handoff note was wrong: the D72 dataset lists existed in code. Now in
  `tests/data/mbr_datasets.json` (`d72_*`, not converted).
- The old ensemble analysis and the new one give the same poles, theory levels and gap ratios on
  the same raw files.
- The 7-1 jobs record other selected occupations than a re-plan with the calibration's saved
  hardware in 14 of 19 realizations (the planner equals the old code on the same inputs). For
  the physics audit.
- `measurement_notebooks/guan/mbramsey.py` and jonginn's
  `qsim_experiments_highkerr_untracked_refactored.ipynb` now fail on the old names (left as they
  are).
